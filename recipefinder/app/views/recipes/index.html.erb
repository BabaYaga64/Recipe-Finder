	<h1>Recipe Finder</h1>

<h3>Searching for <%= @search_term %></h3>

<% if @recipes.nil? %>
	<p>Sorry, we couldn't find any recipes under that search term. Try again.</p>
<% else %>
	<table border="1">
		<tr>
			<th>Thumbnail</th>
			<th>Title</th>
			<th>Social Rank</th>
		</tr>
		<% @recipes.each do |recipe| %>

		<tr class=<%= cycle('even', 'odd') %>>
			<td><%= link_to(image_tag(recipe["image_url"], height: '100', width: '100'), recipe['source_url']) %></td>
			<td><%= link_to(sanitize(recipe["title"]), recipe['source_url']) %></td>
			<td><%= recipe["social_rank"] %></td>
		</tr>
  	<% end %>
	</table>
<% end %>
